package io.critique.critique

/**
 * Holds the broadcast event types
 */
class Events {
    companion object {
        const val ACTION_POST_PUBLISH = "ACTION_POST_PUBLISH"
        const val ACTION_POST_DELETE = "ACTION_POST_DELETE"
    }
}